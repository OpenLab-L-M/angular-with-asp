import { IngredientsFilterPipe } from './ingredients-filter.pipe';

describe('IngredientsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new IngredientsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
